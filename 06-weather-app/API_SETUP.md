# Weather App - API Configuration

## 🔐 Безопасность API ключа

API ключ теперь вынесен в отдельный конфигурационный файл для безопасности.

## 📋 Инструкция по настройке

### Шаг 1: Получите API ключ

1. Перейдите на [OpenWeatherMap](https://openweathermap.org/api)
2. Зарегистрируйтесь (бесплатно)
3. Получите свой API ключ в личном кабинете

### Шаг 2: Настройте приложение

1. Скопируйте файл `config.example.js` и переименуйте в `config.js`:

   ```bash
   cp config.example.js config.js
   ```

2. Откройте `config.js` и замените `your_api_key_here` на ваш API ключ:
   ```javascript
   const CONFIG = {
     WEATHER_API_KEY: "ваш_настоящий_api_ключ",
   };
   ```

### Шаг 3: Запустите приложение

- Откройте `index.html` в браузере
- Начните искать погоду!

## ⚠️ Важно!

- **НЕ** добавляйте файл `config.js` в git
- Файл `config.js` уже в `.gitignore`
- Делитесь только `config.example.js` с другими разработчиками

## 📁 Структура файлов

```
06-weather-app/
├── index.html
├── scripts.js          # Основная логика
├── config.js           # ⚠️ Ваш API ключ (не для git!)
├── config.example.js   # ✅ Пример конфигурации (для git)
├── .gitignore          # Игнорирует config.js
└── .env.example        # Альтернативный пример
```

## 🚀 Как это работает

1. `config.js` содержит ваш секретный API ключ
2. `scripts.js` импортирует конфигурацию: `CONFIG.WEATHER_API_KEY`
3. `.gitignore` не позволяет случайно загрузить ключ в репозиторий

## 🔄 Для других разработчиков

Если вы клонировали этот проект:

1. Скопируйте `config.example.js` → `config.js`
2. Добавьте свой API ключ в `config.js`
3. Готово!
